# Raw execution artifacts

Store real execution output only:

- `load/` — complete `.jtl` plus `html-report/`
- `stress/` — complete `.jtl` plus `html-report/`
- `spike/` — complete `.jtl` plus `html-report/`
- `endurance/` — raw log/report supporting the 10–15 minute threshold claim

Do not replace raw logs with aggregate screenshots.
